$source_file= "<%=@backup_dir%>"
$destination_directory="<%=@store_dir%>"
Get-ChildItem -Path $destination_directory -Include * -Recurse -Force | foreach { Remove-Item $_ -Recurse -Force }
Copy-Item -Path $source_file/* -Destination $destination_directory -Recurse -Force