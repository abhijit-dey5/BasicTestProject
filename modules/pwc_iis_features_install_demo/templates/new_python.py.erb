new_list = <%= @new_array %>

for x in new_list :

  if x<0:

    print ( str(x)+'is is a negative integer')

  elif x == 0:

    print ("why are you giving zero value?")

  elif x>0:

    print ("It's a positive integer")
  else:
    print ("It is not an integer")
  